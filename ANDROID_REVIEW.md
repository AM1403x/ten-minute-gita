# ANDROID_REVIEW.md

> Generated by Agent 4: The Reviewer
> Date: 2026-02-12
> Input: ANDROID_AUDIT.md (31 issues), ANDROID_FIX_PLAN.md (25 fixes, 19 actionable)

---

## Verification Checklist

### Phase 1: Build & Config

| # | Fix | Status | Notes |
|---|-----|--------|-------|
| 1.1 | Add `PLAY_STORE_URL` to `constants/config.ts`, `versionCode` and `playStoreUrl` to `app.json` | **PASS** | `PLAY_STORE_URL` added at line 29 of `constants/config.ts`. `versionCode: 1` at line 38 and `playStoreUrl` at line 40 of `app.json`. All values correct. |
| 1.2 | Bundle 6 font files in `assets/fonts/`, load in `app/_layout.tsx` | **PASS** | All 6 font files present: `NotoSansDevanagari-Regular.ttf`, `NotoSansDevanagari-Bold.ttf`, `NotoSerif-Regular.ttf`, `NotoSerif-Italic.ttf`, `NotoSerif-Bold.ttf`, `NotoSerif-BoldItalic.ttf`. All loaded in `useFonts()` at lines 41-47 of `app/_layout.tsx`. |

### Phase 2: Critical Fixes

| # | Fix | Status | Notes |
|---|-----|--------|-------|
| 2.1 | Create `constants/fonts.ts` with `FONTS` object and `letterSpacingStyle` helper; replace all `fontFamily: 'Georgia'` and `fontFamily: 'System'` | **PASS** | `constants/fonts.ts` exists with correct `FONTS` object (6 font mappings using `Platform.select`) and `letterSpacingStyle` helper. Zero remaining occurrences of `fontFamily: 'Georgia'` or `fontFamily: 'System'` in the codebase. All 10 files updated to use `FONTS.serif`, `FONTS.serifItalic`, or `FONTS.devanagari` as appropriate. |
| 2.2 | `AuthSheet.tsx` `onRequestClose` uses `() => {}` not `undefined` when non-dismissible | **PASS** | Line 96: `onRequestClose={dismissible ? handleDismiss : () => {}}` -- correct. |
| 2.4 | `AuthSheet.tsx` `paddingBottom` uses Platform.OS check | **PASS** | Line 211: `paddingBottom: Platform.OS === 'ios' ? 44 : 24` -- correct. |

### Phase 3: High-Priority Fixes

| # | Fix | Status | Notes |
|---|-----|--------|-------|
| 3.1 | BackHandler in `app/reading/[id].tsx` for full player minimize/dismiss | **PASS** | Lines 376-393: `useEffect` with `Platform.OS !== 'android'` guard, handles `full` (minimize) and `mini` (dismiss) player states, cleans up subscription. Both `BackHandler` and `Platform` imported. |
| 3.2 | BackHandler in `app/(tabs)/_layout.tsx` for double-back-to-exit | **PASS** | Lines 22-41: `lastBackPress` ref + `useFocusEffect` with `BackHandler`, `ToastAndroid.show` on first press, exit on second press within 2s. All imports present. |
| 3.3 | BackHandler in `app/completed-readings.tsx` for keyboard dismiss | **PASS** | Lines 85-96: `useEffect` with `Platform.OS !== 'android'` guard, calls `Keyboard.dismiss()` on back press, returns `false` to allow default navigation. All imports present. |
| 3.4 | `SupportSection.tsx` uses `market://details?id=` for Android, fallback uses `PLAY_STORE_URL` | **PASS** | Lines 19-23: `Platform.select` with `ios`, `android: 'market://details?id=com.tenminutegita.app'`, and `default: CONFIG.APP_STORE_URL`. Fallback at line 28: `Platform.OS === 'android' ? CONFIG.PLAY_STORE_URL : CONFIG.APP_STORE_URL`. |
| 3.5 | All 13 `letterSpacing` usages replaced with `...letterSpacingStyle(N)` | **PASS** | Verified all 13 files. Only remaining `letterSpacing:` occurrence is the definition inside `letterSpacingStyle` itself in `constants/fonts.ts`. All component files use the spread syntax. |
| 3.6 | All bare `console.log` in `AudioPlayerContext.tsx` replaced with `logger.warn` | **PARTIAL** | `AudioPlayerContext.tsx` has zero `console.log` calls -- all removed or replaced. However, `app/reading/[id].tsx` still has 5 bare `console.log('[AUDIO_DEBUG]...')` calls at lines 265, 308, 323, 364, and 367. The fix plan explicitly stated these should also be replaced ("Apply the same pattern to the 3 bare `console.log` calls in `app/reading/[id].tsx`"). |

### Phase 4: Medium Fixes

| # | Fix | Status | Notes |
|---|-----|--------|-------|
| 4.1 | `underlineColorAndroid="transparent"` on all TextInput components | **PASS** | Found 11 occurrences across: `app/completed-readings.tsx` (1), `app/auth/login.tsx` (3), `components/settings/DevTools.tsx` (1), `components/snippet/FormattedText.tsx` (1), `components/snippet/VerseBlock.tsx` (2), `components/snippet/HighlightText.tsx` (1), `components/audio/AudioVerseBlock.tsx` (2). All TextInput components covered, including VerseBlock and AudioVerseBlock selectable text inputs. |
| 4.2 | Devanagari lineHeight uses `Platform.OS === 'android' ? 1.8 : 1.5` in VerseBlock and AudioVerseBlock | **PASS** | `VerseBlock.tsx` line 43: `lineHeight: (fontSize + 3) * (Platform.OS === 'android' ? 1.8 : 1.5)`. `AudioVerseBlock.tsx` line 54: same pattern. Both files import `Platform`. The highlighted text path in AudioVerseBlock (line 70) already uses 1.8 unconditionally. |
| 4.3 | AbortController timeout on CDN fetch in `utils/audioSource.ts` | **PASS** | Lines 76-89: `AbortController` created with 15s timeout via `setTimeout`. `fetch` uses `{ signal: controller.signal }`. `clearTimeout` in both success and catch paths. |
| 4.4 | Polling interval `Platform.OS === 'android' ? 100 : 50` in `useAudioPosition.ts` | **PASS** | Line 72: `const pollInterval = Platform.OS === 'android' ? 100 : 50;` Line 73: `const interval = setInterval(poll, pollInterval);`. `Platform` imported from `react-native`. |
| 4.6 | `SWIPE_ACTIVE_OFFSET_X` uses Platform.OS check in `constants/config.ts` | **PASS** | Line 8: `SWIPE_ACTIVE_OFFSET_X: Platform.OS === 'android' ? 50 : 30`. `Platform` imported at line 1. |

---

## Additional Checks

| Check | Status | Notes |
|-------|--------|-------|
| Any remaining `console.log` in `AudioPlayerContext.tsx`? | **PASS** | Zero occurrences found. |
| Any remaining `fontFamily: 'Georgia'` in tsx/ts files? | **PASS** | Zero occurrences found. |
| Any remaining `fontFamily: 'System'` in tsx/ts files? | **PASS** | Zero occurrences found. |
| Does `npx tsc --noEmit` pass? | **PASS** | TypeScript type checker completes with zero errors. |

---

## Issues Found

### 1. `console.log` calls remaining in `app/reading/[id].tsx`

**Severity: Medium**

The fix plan (Fix 3.6) explicitly states: "Apply the same pattern to the 3 bare `console.log` calls in `app/reading/[id].tsx` (lines 263, 306, 321, 362, 365)."

Five bare `console.log` calls remain in this file:

- Line 265: `console.log('[AUDIO_DEBUG] pill state load: snippetId=', ...)`
- Line 308: `console.log('[AUDIO_DEBUG] handleListenPress: pillState=', ...)`
- Line 323: `console.log('[AUDIO_DEBUG] auto-play triggering for snippetId=', ...)`
- Line 364: `console.log('[AUDIO_DEBUG] reading screen FOCUSED, snippetId=', ...)`
- Line 367: `console.log('[AUDIO_DEBUG] reading screen BLUR, playerState=', ...)`

These will cross the native bridge on Android in production, causing performance overhead. They should be replaced with `logger.warn('ReadingScreen', ...)` or removed entirely.

### 2. Fix 4.5 (android_ripple) and Fix 4.7 (dynamic safe area padding) not verified

The fix plan listed Fix 4.5 (add `android_ripple` to key Pressable components) and Fix 4.7 (replace hardcoded `paddingBottom: 80` with dynamic inset). These were part of the 19 actionable fixes. Quick check shows:
- **Fix 4.5 (android_ripple):** No `android_ripple` props found in any files. This fix was NOT implemented. However, it is more of a polish item and was listed as medium priority.
- **Fix 4.7 (safe area padding):** `SnippetContent.tsx` line 450 still shows `paddingBottom: 80` in the static styles. The reading screen does pass `contentPaddingBottom` dynamically via measurement, so the static fallback is less critical. This fix was NOT implemented.

---

## Summary

| Category | Count |
|----------|-------|
| Fixes verified as PASS | 14 of 16 checked |
| Fixes with partial issues | 1 (Fix 3.6 -- AudioPlayerContext clean, but reading screen still has console.log) |
| Fixes NOT implemented | 2 (Fix 4.5 android_ripple, Fix 4.7 dynamic safe area padding) |
| TypeScript compilation | PASS (zero errors) |

### Fixes Fully Implemented (14/19)
- 1.1, 1.2, 2.1, 2.2, 2.4, 3.1, 3.2, 3.3, 3.4, 3.5, 4.1, 4.2, 4.3, 4.4, 4.6

### Partially Implemented (1/19)
- 3.6 -- AudioPlayerContext.tsx fully cleaned, but `app/reading/[id].tsx` still has 5 bare `console.log` calls

### Not Implemented (2/19)
- 4.5 -- No `android_ripple` added to Pressable components
- 4.7 -- No `useSafeAreaInsets` added to `SnippetContent.tsx`

### Already Handled / No Change Needed (1/19)
- 2.3 -- Backdrop dismiss gating already correct (noted in fix plan as "no change needed")

---

## Overall Readiness Assessment

**The app is in good shape for Android launch.** The critical and high-priority fixes are all implemented correctly:

- Fonts: All `Georgia` and `System` font references replaced with cross-platform `FONTS` object backed by bundled Noto fonts. Devanagari rendering will work reliably.
- Back button: All three critical screens (reading, tabs, completed-readings) have proper `BackHandler` integration.
- Auth: Modal `onRequestClose` and platform padding fixed.
- Store links: Rate App correctly routes to Google Play with `market://` intent and fallback.
- Audio: CDN timeout, polling interval reduction, and console.log cleanup in AudioPlayerContext all done.
- Gestures: Swipe threshold increased for Android to avoid system gesture conflicts.
- TextInput: All underlines suppressed.
- Letter spacing: All 13 occurrences use the clipping-safe helper.

**Remaining items before launch:**
1. **Required:** Replace 5 `console.log` calls in `app/reading/[id].tsx` with `logger.warn` (trivial, 5 minutes).
2. **Recommended:** Add `android_ripple` to key interactive elements (Fix 4.5) for native feel. Not blocking.
3. **Optional:** Add `useSafeAreaInsets` fallback in `SnippetContent.tsx` (Fix 4.7). The reading screen already measures dynamically, so this is low risk.

---

*This review is read-only. No code was modified.*
