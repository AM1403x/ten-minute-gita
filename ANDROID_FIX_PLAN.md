# ANDROID_FIX_PLAN.md

> Generated by Agent 2: The Architect
> Input: ANDROID_AUDIT.md (31 issues: 4 critical, 9 high, 15 medium, 3 low)

---

## Phase 1: Build & Config

These fixes establish the foundation. Nothing else can be tested until the app builds and configures correctly on Android.

### Fix 1.1: Add Play Store URL and versionCode to config

- **File(s):** `constants/config.ts`, `app.json`
- **Current (`constants/config.ts:25-26`):**
  ```ts
  APP_STORE_ID: '6758332047',
  APP_STORE_URL: 'https://apps.apple.com/app/10-minute-gita/id6758332047',
  ```
- **Change to:**
  ```ts
  APP_STORE_ID: '6758332047',
  APP_STORE_URL: 'https://apps.apple.com/app/10-minute-gita/id6758332047',
  PLAY_STORE_URL: 'https://play.google.com/store/apps/details?id=com.tenminutegita.app',
  ```
- **Current (`app.json:36-43`):**
  ```json
  "android": {
    "package": "com.tenminutegita.app",
    "googleServicesFile": "./google-services.json",
    "adaptiveIcon": {
      "foregroundImage": "./assets/images/adaptive-icon.png",
      "backgroundColor": "#FAFAFA"
    },
    "edgeToEdgeEnabled": true
  }
  ```
- **Change to:**
  ```json
  "android": {
    "package": "com.tenminutegita.app",
    "versionCode": 1,
    "googleServicesFile": "./google-services.json",
    "playStoreUrl": "https://play.google.com/store/apps/details?id=com.tenminutegita.app",
    "adaptiveIcon": {
      "foregroundImage": "./assets/images/adaptive-icon.png",
      "backgroundColor": "#FAFAFA"
    },
    "edgeToEdgeEnabled": true
  }
  ```
- **Why:** Android needs a Play Store URL for store-related features and a versionCode for Google Play uploads. Covers audit issues 3.3, 3.5, 7.6.
- **Depends on:** None
- **Complexity:** S

### Fix 1.2: Bundle Noto Sans Devanagari and Noto Serif fonts

- **File(s):** `app/_layout.tsx`, new font files in `assets/fonts/`
- **New files needed:**
  - `assets/fonts/NotoSansDevanagari-Regular.ttf` — download from [Google Fonts](https://fonts.google.com/noto/specimen/Noto+Sans+Devanagari)
  - `assets/fonts/NotoSansDevanagari-Bold.ttf`
  - `assets/fonts/NotoSerif-Regular.ttf` — download from [Google Fonts](https://fonts.google.com/specimen/Noto+Serif)
  - `assets/fonts/NotoSerif-Italic.ttf`
  - `assets/fonts/NotoSerif-Bold.ttf`
  - `assets/fonts/NotoSerif-BoldItalic.ttf`
- **Commands:** Download fonts manually from Google Fonts and place in `assets/fonts/`. No npm install needed (app already has `expo-font` v14.0.11).
- **Current (`app/_layout.tsx:39-42`):**
  ```ts
  const [loaded, error] = useFonts({
    SpaceMono: require('../assets/fonts/SpaceMono-Regular.ttf'),
    ...FontAwesome.font,
  });
  ```
- **Change to:**
  ```ts
  const [loaded, error] = useFonts({
    SpaceMono: require('../assets/fonts/SpaceMono-Regular.ttf'),
    'NotoSansDevanagari-Regular': require('../assets/fonts/NotoSansDevanagari-Regular.ttf'),
    'NotoSansDevanagari-Bold': require('../assets/fonts/NotoSansDevanagari-Bold.ttf'),
    'NotoSerif-Regular': require('../assets/fonts/NotoSerif-Regular.ttf'),
    'NotoSerif-Italic': require('../assets/fonts/NotoSerif-Italic.ttf'),
    'NotoSerif-Bold': require('../assets/fonts/NotoSerif-Bold.ttf'),
    'NotoSerif-BoldItalic': require('../assets/fonts/NotoSerif-BoldItalic.ttf'),
    ...FontAwesome.font,
  });
  ```
- **Why:** Android doesn't have Georgia or guaranteed Devanagari support. Bundling fonts ensures consistent rendering. Covers audit issues 2.1, 2.2.
- **Depends on:** None
- **Complexity:** M

---

## Phase 2: Critical Fixes

These fixes address app-breaking issues that would make the app unusable on Android.

### Fix 2.1: Replace `fontFamily: 'Georgia'` with cross-platform serif font

This fix must apply to all 10 files that use Georgia. After Fix 1.2 bundles the fonts, this fix references them via `Platform.select`.

- **File(s):** Create a new constants file `constants/fonts.ts` and update 8 component files.

- **New file `constants/fonts.ts`:**
  ```ts
  import { Platform } from 'react-native';

  export const FONTS = {
    serif: Platform.select({
      ios: 'Georgia',
      default: 'NotoSerif-Regular',
    }),
    serifItalic: Platform.select({
      ios: 'Georgia',
      default: 'NotoSerif-Italic',
    }),
    serifBold: Platform.select({
      ios: 'Georgia',
      default: 'NotoSerif-Bold',
    }),
    serifBoldItalic: Platform.select({
      ios: 'Georgia',
      default: 'NotoSerif-BoldItalic',
    }),
    devanagari: Platform.select({
      ios: 'System',
      default: 'NotoSansDevanagari-Regular',
    }),
    devanagariBold: Platform.select({
      ios: 'System',
      default: 'NotoSansDevanagari-Bold',
    }),
  } as const;
  ```

- **File: `components/SnippetContent.tsx`**
  - Add import: `import { FONTS } from '@/constants/fonts';`
  - Line 453 (`title` style): change `fontFamily: 'Georgia'` to `fontFamily: FONTS.serif`
  - Line 457 (`sanskritPreview` style): change `fontFamily: 'System'` to `fontFamily: FONTS.devanagari`
  - Line 467 (`reflectionText` style): change `fontFamily: 'Georgia'` to `fontFamily: FONTS.serifItalic`

- **File: `components/snippet/VerseBlock.tsx`**
  - Add import: `import { FONTS } from '@/constants/fonts';`
  - Line 76 (`sanskritText` style): change `fontFamily: 'System'` to `fontFamily: FONTS.devanagari`
  - Line 79 (`translationText` style): change `fontFamily: 'Georgia'` to `fontFamily: FONTS.serif`

- **File: `components/snippet/Paragraph.tsx`**
  - Add import: `import { FONTS } from '@/constants/fonts';`
  - Line 36 (`paragraph` style): change `fontFamily: 'Georgia'` to `fontFamily: FONTS.serif`

- **File: `components/audio/AudioVerseBlock.tsx`**
  - Add import: `import { FONTS } from '@/constants/fonts';`
  - Line 158 (`sanskritText` style): change `fontFamily: 'System'` to `fontFamily: FONTS.devanagari`
  - Line 161 (`translationText` style): change `fontFamily: 'Georgia'` to `fontFamily: FONTS.serif`

- **File: `components/TodayCard.tsx`**
  - Add import: `import { FONTS } from '@/constants/fonts';`
  - Line 168 (`title` style): change `fontFamily: 'Georgia'` to `fontFamily: FONTS.serif`
  - Line 242 (`upNextTitle` style): change `fontFamily: 'Georgia'` to `fontFamily: FONTS.serif`

- **File: `components/share/ShareCard.tsx`**
  - Add import: `import { FONTS } from '@/constants/fonts';`
  - Line 105 (`mainText` style): change `fontFamily: 'Georgia'` to `fontFamily: FONTS.serif`

- **File: `components/ReflectionTeaser.tsx`**
  - Add import: `import { FONTS } from '@/constants/fonts';`
  - Line 80 (`text` style): change `fontFamily: 'Georgia'` to `fontFamily: FONTS.serifItalic`

- **File: `components/settings/ReadingPreferences.tsx`**
  - Add import: `import { FONTS } from '@/constants/fonts';`
  - Line 146 (`previewTranslation` style): change `fontFamily: 'Georgia'` to `fontFamily: FONTS.serif`

- **Why:** Georgia is not available on Android. Noto Serif is the closest Google-ecosystem equivalent. Covers audit issues 2.1, 2.2, 2.3.
- **Depends on:** Fix 1.2 (fonts must be bundled first)
- **Complexity:** M

### Fix 2.2: Auth gate modal must handle Android back button when mandatory

- **File(s):** `components/auth/AuthSheet.tsx`
- **Current (line 96):**
  ```tsx
  onRequestClose={dismissible ? handleDismiss : undefined}
  ```
- **Change to:**
  ```tsx
  onRequestClose={dismissible ? handleDismiss : () => {}}
  ```
- **Why:** On Android, `onRequestClose={undefined}` on a Modal causes unpredictable behavior when the hardware back button is pressed. A no-op function prevents the modal from being dismissed. Covers audit issue 1.1.
- **Depends on:** None
- **Complexity:** S

### Fix 2.3: Auth gate backdrop must not dismiss when mandatory

- **File(s):** `components/auth/AuthSheet.tsx`
- **Current (line 99-103):**
  ```tsx
  <Pressable
    style={StyleSheet.absoluteFill}
    onPress={dismissible ? handleDismiss : undefined}
  >
    <View style={[StyleSheet.absoluteFill, styles.overlayTint]} />
  </Pressable>
  ```
- **Note:** This is already correctly gated. No change needed. Included for completeness.
- **Depends on:** None
- **Complexity:** S (already handled)

### Fix 2.4: Fix AuthSheet bottom padding for Android

- **File(s):** `components/auth/AuthSheet.tsx`
- **Current (line 211 in styles):**
  ```ts
  paddingBottom: 44,
  ```
- **Change to:**
  ```ts
  paddingBottom: Platform.OS === 'ios' ? 44 : 24,
  ```
- **Why:** 44px is the iOS home indicator height. Android has no equivalent bar at that position. Covers audit issue 10.3.
- **Depends on:** None
- **Complexity:** S

---

## Phase 3: High-Priority Fixes

These fixes address major UX issues that affect core functionality.

### Fix 3.1: Add BackHandler for full player overlay

- **File(s):** `app/reading/[id].tsx`
- **Current:** No BackHandler import or usage in the file.
- **Add import at top:**
  ```ts
  import { BackHandler } from 'react-native';
  ```
  (Add to existing react-native import line 6)
- **Add after the `useFocusEffect` block (around line 371), add a new effect:**
  ```ts
  // Android back button: minimize full player instead of navigating away
  useEffect(() => {
    if (Platform.OS !== 'android') return;

    const onBackPress = () => {
      if (audioUIState.playerState === 'full') {
        minimizePlayer();
        return true; // consume the event
      }
      if (audioUIState.playerState === 'mini') {
        dismissPlayer();
        return true; // consume the event
      }
      return false; // let default behavior handle it
    };

    const subscription = BackHandler.addEventListener('hardwareBackPress', onBackPress);
    return () => subscription.remove();
  }, [audioUIState.playerState, minimizePlayer, dismissPlayer]);
  ```
- **Also add `Platform` to the existing react-native import** (line 7, add Platform if not there — currently it is NOT imported in this file).
- **Why:** Without this, Android back button navigates away from the reading screen even when the full player is open, orphaning audio. Covers audit issues 1.2 and 1.3.
- **Depends on:** None
- **Complexity:** M

### Fix 3.2: Add BackHandler for tab navigator (exit guard)

- **File(s):** `app/(tabs)/_layout.tsx`
- **Add imports:**
  ```ts
  import { BackHandler, Platform, ToastAndroid } from 'react-native';
  import { useRef, useEffect } from 'react';
  import { useFocusEffect } from '@react-navigation/native';
  import { useCallback } from 'react';
  ```
- **Inside `TabLayout`, before the return statement, add:**
  ```ts
  const lastBackPress = useRef(0);

  useFocusEffect(
    useCallback(() => {
      if (Platform.OS !== 'android') return;

      const onBackPress = () => {
        const now = Date.now();
        if (now - lastBackPress.current < 2000) {
          return false; // let the app exit
        }
        lastBackPress.current = now;
        ToastAndroid.show('Press back again to exit', ToastAndroid.SHORT);
        return true; // consume the event
      };

      const subscription = BackHandler.addEventListener('hardwareBackPress', onBackPress);
      return () => subscription.remove();
    }, [])
  );
  ```
- **Why:** Standard Android pattern to prevent accidental app exit. Covers audit issue 1.4.
- **Depends on:** None
- **Complexity:** M

### Fix 3.3: Dismiss keyboard before back navigation on completed-readings

- **File(s):** `app/completed-readings.tsx`
- **Add imports (to existing react-native import):** `BackHandler, Platform`
- **Add after the `useLayoutEffect` block (line 79), add:**
  ```ts
  // Android: dismiss keyboard on back press before navigating
  useEffect(() => {
    if (Platform.OS !== 'android') return;

    const onBackPress = () => {
      // If keyboard is up (search input focused), dismiss it first
      Keyboard.dismiss();
      return false; // let default back navigation proceed
    };

    const subscription = BackHandler.addEventListener('hardwareBackPress', onBackPress);
    return () => subscription.remove();
  }, []);
  ```
- **Why:** On Android, pressing back with the keyboard open should dismiss the keyboard first. Covers audit issue 1.5.
- **Depends on:** None
- **Complexity:** S

### Fix 3.4: Fix Rate App to open correct store per platform

- **File(s):** `components/settings/SupportSection.tsx`
- **Add import:** `import { CONFIG } from '@/constants/config';` (already imported)
- **Current (lines 18-21):**
  ```ts
  const storeUrl = Platform.select({
    ios: `itms-apps://itunes.apple.com/app/id${CONFIG.APP_STORE_ID}?action=write-review`,
    default: CONFIG.APP_STORE_URL,
  });
  ```
- **Change to:**
  ```ts
  const storeUrl = Platform.select({
    ios: `itms-apps://itunes.apple.com/app/id${CONFIG.APP_STORE_ID}?action=write-review`,
    android: `market://details?id=com.tenminutegita.app`,
    default: CONFIG.APP_STORE_URL,
  });
  ```
- **Also update the fallback on line 26:**
  ```ts
  // Current
  await Linking.openURL(CONFIG.APP_STORE_URL);
  // Change to
  await Linking.openURL(Platform.OS === 'android' ? CONFIG.PLAY_STORE_URL : CONFIG.APP_STORE_URL);
  ```
- **Why:** Android users were being sent to the Apple App Store. Covers audit issues 3.2 and 3.3.
- **Depends on:** Fix 1.1 (PLAY_STORE_URL must exist)
- **Complexity:** S

### Fix 3.5: Fix `letterSpacing` clipping on Android

- **File(s):** 13 files with letterSpacing, plus a shared utility.
- **Approach:** Add `paddingRight` equal to `letterSpacing` value on Android only, using a helper function.
- **Add to `constants/fonts.ts` (created in Fix 2.1):**
  ```ts
  /**
   * Android applies letterSpacing after the last character, causing clipping.
   * Add paddingRight on Android to compensate.
   */
  export function letterSpacingStyle(spacing: number) {
    return {
      letterSpacing: spacing,
      ...(Platform.OS === 'android' ? { paddingRight: spacing } : {}),
    };
  }
  ```
- **Files to update (replace `letterSpacing: N` with spread `...letterSpacingStyle(N)`):**
  1. `components/SnippetContent.tsx:451` — `letterSpacing: 2` in `chapterLabel`
  2. `components/SnippetContent.tsx:466` — `letterSpacing: 1` in `reflectionTitle`
  3. `components/TodayCard.tsx:160` — `letterSpacing: 1.5` in `chapterLabel`
  4. `components/TodayCard.tsx:232` — `letterSpacing: 2` in `upNextLabel`
  5. `components/ReflectionTeaser.tsx:75` — `letterSpacing: 1` in `label`
  6. `components/settings/SupportSection.tsx:135` — `letterSpacing: 1` in `sectionHeader`
  7. `components/settings/ReadingPreferences.tsx:121` — `letterSpacing: 1` in `sectionHeader`
  8. `components/settings/AppearanceSettings.tsx:68` — `letterSpacing: 1` in `sectionHeader`
  9. `components/settings/DevTools.tsx:121` — `letterSpacing: 1` in `sectionHeader`
  10. `components/settings/NotificationSettings.tsx:173` — `letterSpacing: 1` in `sectionHeader`
  11. `components/downloads/DownloadManager.tsx:140` — `letterSpacing: 1` in `sectionHeader`
  12. `app/auth/login.tsx:371` — `letterSpacing: 0.8` in `label`
  13. `app/completed-readings.tsx:386` — `letterSpacing: 0.5` in `matchSource`

  For each file: import `letterSpacingStyle` from `@/constants/fonts`, then in the relevant style object, replace:
  ```ts
  letterSpacing: 1,
  ```
  with:
  ```ts
  ...letterSpacingStyle(1),
  ```
  Note: `StyleSheet.create` requires static objects. The helper returns a plain object, so these styles may need to move to inline styles or the `StyleSheet.create` call needs to accept computed values (which it does in RN). Alternatively, since `Platform.OS` is constant at runtime, `letterSpacingStyle(1)` evaluates once at module load, so it IS safe to spread into `StyleSheet.create`.

- **Why:** Android applies letterSpacing after the last character, causing the rightmost character to be clipped. Covers audit issue 2.4.
- **Depends on:** Fix 2.1 (fonts.ts must exist)
- **Complexity:** M

### Fix 3.6: Replace bare `console.log` with `logger` in AudioPlayerContext

- **File(s):** `contexts/AudioPlayerContext.tsx`
- **Current:** 13+ bare `console.log` calls (lines 178, 180, 216, 248, 253, 336, 425, 435, 445, 454, 460, 478, 611).
- **Change:** Replace every `console.log('[AUDIO_DEBUG]` with `logger.warn('AudioPlayer',` or remove them entirely. The `logger.ts` utility guards behind `__DEV__`, so these will be no-ops in production.
- **Pattern for each occurrence:**
  ```ts
  // Current
  console.log('[AUDIO_DEBUG] autoSave: time=', time.toFixed(1), 'snippetId=', sid);
  // Change to
  logger.warn('AudioPlayer', `autoSave: time=${time.toFixed(1)} snippetId=${sid}`);
  ```
- **Apply the same pattern to the 3 bare `console.log` calls in `app/reading/[id].tsx`** (lines 263, 306, 321, 362, 365).
- **Why:** Bare `console.log` crosses the native bridge on Android in production, causing performance overhead. Covers audit issue 4.5.
- **Depends on:** None
- **Complexity:** M

---

## Phase 4: Medium Fixes

These fixes address platform conformance and polish issues.

### Fix 4.1: Suppress TextInput underline on Android

- **File(s):** `app/completed-readings.tsx`, `app/auth/login.tsx`, `components/settings/DevTools.tsx`
- **For each TextInput, add prop:** `underlineColorAndroid="transparent"`

  **`app/completed-readings.tsx` (line 273-281):**
  ```tsx
  <TextInput
    style={[styles.searchInput, { color: colors.text }]}
    placeholder={t('completedList.searchPlaceholder')}
    placeholderTextColor={colors.textSecondary}
    value={query}
    onChangeText={handleQueryChange}
    autoCorrect={false}
    autoCapitalize="none"
    returnKeyType="search"
    underlineColorAndroid="transparent"
  />
  ```

  **`app/auth/login.tsx` — 3 TextInputs (lines 204, 227, 250):**
  Add `underlineColorAndroid="transparent"` to each.

  **`components/settings/DevTools.tsx` (line 71-85):**
  Add `underlineColorAndroid="transparent"` to the simulate day TextInput.

  **Also:** `components/snippet/VerseBlock.tsx` and `components/audio/AudioVerseBlock.tsx` use `TextInput` with `editable={false}` for selectable Sanskrit text. These also need `underlineColorAndroid="transparent"`.

- **Why:** Android TextInput shows a bottom underline by default that conflicts with custom styling. Covers audit issue 5.4.
- **Depends on:** None
- **Complexity:** S

### Fix 4.2: Increase Devanagari lineHeight for Android

- **File(s):** `components/snippet/VerseBlock.tsx`, `components/audio/AudioVerseBlock.tsx`
- **Current (VerseBlock.tsx line 41):**
  ```ts
  { color: colors.text, fontSize: fontSize + 3, lineHeight: (fontSize + 3) * 1.5, padding: 0 }
  ```
- **Change to:**
  ```ts
  { color: colors.text, fontSize: fontSize + 3, lineHeight: (fontSize + 3) * (Platform.OS === 'android' ? 1.8 : 1.5), padding: 0 }
  ```
- **Apply same pattern to AudioVerseBlock.tsx line 52:**
  ```ts
  { color: colors.text, fontSize: fontSize + 3, lineHeight: (fontSize + 3) * (Platform.OS === 'android' ? 1.8 : 1.5), padding: 0 }
  ```
  (Note: AudioVerseBlock already uses 1.8 for the highlighted text path at line 68 but uses 1.5 for the non-highlighted path at line 52.)
- **Add `Platform` import** to both files.
- **Why:** Devanagari matras extend above/below baseline more than Latin characters. 1.5x lineHeight clips them on Android. Covers audit issue 2.5.
- **Depends on:** None
- **Complexity:** S

### Fix 4.3: Add CDN fetch timeout with AbortController

- **File(s):** `utils/audioSource.ts`
- **Current (line 77):**
  ```ts
  const response = await fetch(cdnUrl);
  ```
- **Change to:**
  ```ts
  const controller = new AbortController();
  const timeoutId = setTimeout(() => controller.abort(), 15000);
  try {
    const response = await fetch(cdnUrl, { signal: controller.signal });
    clearTimeout(timeoutId);
    if (response.ok) {
      const data = await response.json() as AlignedData;
      alignedJsonCache[cacheKey] = data;
      return data;
    }
  } catch (e) {
    clearTimeout(timeoutId);
    // CDN fetch failed or timed out
  }
  ```
  Replace the existing try/catch block around the CDN fetch (lines 76-85).
- **Why:** On slow Android networks (2G/3G common in India), fetch can hang indefinitely. 15-second timeout prevents the app from appearing frozen. Covers audit issue 8.3.
- **Depends on:** None
- **Complexity:** S

### Fix 4.4: Increase audio position polling interval for Android

- **File(s):** `hooks/useAudioPosition.ts`
- **Add import:** `import { Platform } from 'react-native';`
- **Current (line 71):**
  ```ts
  const interval = setInterval(poll, 50);
  ```
- **Change to:**
  ```ts
  const pollInterval = Platform.OS === 'android' ? 100 : 50;
  const interval = setInterval(poll, pollInterval);
  ```
- **Why:** 20 polls/sec crossing the JS-native bridge causes jank on budget Android devices. 10 polls/sec (100ms) is sufficient for smooth-enough word highlighting while reducing bridge traffic by 50%. Covers audit issues 4.4 and 9.1.
- **Depends on:** None
- **Complexity:** S

### Fix 4.5: Add `android_ripple` to key Pressable components

- **File(s):** Multiple component files. Apply to the most visible interactive elements only (not all 50+ Pressables).
- **Pattern:** Replace `opacity` press feedback with `android_ripple` on Android while keeping opacity on iOS.
  ```tsx
  <Pressable
    style={({ pressed }) => [styles.actionRow, Platform.OS === 'ios' && { opacity: pressed ? 0.7 : 1 }]}
    android_ripple={{ color: 'rgba(0,0,0,0.1)' }}
    onPress={...}
  >
  ```
- **Priority files (most visible):**
  1. `components/settings/SupportSection.tsx` — Rate App, Share, Feedback rows (3 Pressables)
  2. `app/completed-readings.tsx` — Search result rows
  3. `components/TodayCard.tsx` — CTA button, preview button
  4. `components/reading/NavigationControls.tsx` — Previous/Next/Mark Complete buttons

- **Why:** Android users expect Material ripple feedback on interactive elements. Covers audit issue 5.3.
- **Depends on:** None
- **Complexity:** M

### Fix 4.6: Increase swipe threshold for Android gesture navigation

- **File(s):** `constants/config.ts`
- **Current (line 6):**
  ```ts
  SWIPE_ACTIVE_OFFSET_X: 30,
  ```
- **Change to:**
  ```ts
  SWIPE_ACTIVE_OFFSET_X: Platform.OS === 'android' ? 50 : 30,
  ```
- **Add import:** `import { Platform } from 'react-native';` at top of file.
- **Why:** Android 10+ edge-swipe-to-go-back gesture conflicts with the app's horizontal swipe navigation. A higher threshold (50px) makes it less likely to trigger accidentally. Covers audit issue 6.3.
- **Depends on:** None
- **Complexity:** S

### Fix 4.7: Replace hardcoded `paddingBottom: 80` with dynamic inset

- **File(s):** `components/SnippetContent.tsx`
- **Current (line 449):**
  ```ts
  contentContainer: { paddingHorizontal: 20, paddingTop: 20, paddingBottom: 80 },
  ```
- **Note:** The `paddingBottom` is already overridable via the `contentPaddingBottom` prop (line 232). The reading screen passes this value calculated from `bottomAreaH` measurement. The default `80` is only a fallback.
- **Change:** This is actually safe as-is because the reading screen measures `bottomAreaH` dynamically. However, the SnippetContent should use `useSafeAreaInsets` for the default:
  ```tsx
  // Add import
  import { useSafeAreaInsets } from 'react-native-safe-area-context';

  // Inside SnippetContent component, before return:
  const insets = useSafeAreaInsets();
  const defaultPaddingBottom = 80 + insets.bottom;

  // In contentContainerStyle:
  contentContainerStyle={[
    styles.contentContainer,
    { paddingBottom: contentPaddingBottom ?? defaultPaddingBottom },
  ]}
  ```
  Remove the hardcoded `paddingBottom: 80` from styles.contentContainer.
- **Why:** With `edgeToEdgeEnabled: true`, content may overlap the Android navigation bar. Using `useSafeAreaInsets` adds proper bottom inset. Covers audit issue 10.2.
- **Depends on:** None
- **Complexity:** S

---

## Phase 5: Low-Priority & Deferred

These are nice-to-haves or items that require significant effort relative to their impact.

### Fix 5.1: Background audio and lock screen controls on Android (DEFERRED)

- **File(s):** `contexts/AudioPlayerContext.tsx`, `app.json`
- **Status:** DEFERRED for post-launch. Here's the analysis:

  **Option A: expo-audio built-in capabilities**
  - `expo-audio`'s `setAudioModeAsync({ shouldPlayInBackground: true })` is already called (line 189-195).
  - On Android, expo-audio (SDK 54) uses Android's `MediaPlayer` which should support background playback if the app has foreground service permission.
  - The `expo-audio` plugin is already in `app.json:60` (`"expo-audio"`).
  - With Expo SDK 54, expo-audio on Android requires adding audio foreground service config. Add to `app.json` plugins:
    ```json
    [
      "expo-audio",
      {
        "microphonePermission": false
      }
    ]
    ```
  - Test whether expo-audio background playback works on Android with this config before considering migration.

  **Option B: react-native-track-player**
  - Full-featured media playback library with built-in notification controls, lock screen integration, and audio focus management.
  - Requires ejecting from managed Expo workflow or using a config plugin.
  - Significant migration effort: rewrite AudioPlayerContext, remove expo-audio dependency.
  - NOT recommended for initial Android launch.

  **Recommendation:** Test expo-audio's Android background playback first. If it doesn't work, file this as a fast-follow issue. Lock screen controls (`setActiveForLockScreen`) may not work on Android with expo-audio -- wrap in try/catch (already done).

- **Covers:** Audit issues 3.4, 3.6, 4.1, 4.2.
- **Depends on:** None
- **Complexity:** L (if migration needed)

### Fix 5.2: Audio focus and headphone disconnect handling (DEFERRED)

- **File(s):** `contexts/AudioPlayerContext.tsx`
- **Status:** DEFERRED for post-launch.
- **Notes:**
  - `expo-audio`'s `setAudioModeAsync({ interruptionMode: 'doNotMix' })` is already set (line 191). This SHOULD handle audio focus on Android for basic cases (pause when phone call comes in).
  - For headphone disconnect (`ACTION_AUDIO_BECOMING_NOISY`), expo-audio may or may not handle this on Android. This requires testing.
  - If expo-audio doesn't handle these, a native module or `react-native-track-player` migration would be needed.
- **Covers:** Audit issues 4.1, 4.2.
- **Complexity:** L

### Fix 5.3: Add ProGuard keep rules for Firebase

- **File(s):** Create `android/app/proguard-rules.pro` (only relevant after `npx expo prebuild`)
- **Alternative:** Add to `app.json` android section:
  ```json
  "proguardRules": [
    "-keep class com.google.firebase.** { *; }",
    "-keep class com.google.android.gms.** { *; }"
  ]
  ```
  Note: This field may not be supported in Expo managed workflow. The EAS Build default ProGuard config typically handles Firebase. Only add if production crashes indicate code stripping issues.
- **Status:** DEFERRED unless crash reports indicate need.
- **Covers:** Audit issue 7.8.
- **Complexity:** S

### Fix 5.4: `fontWeight: '800'` fallback

- **File(s):** `components/TodayCard.tsx:230`, `app/auth/login.tsx:388,400`
- **Status:** Low priority. Android Roboto will silently fall back to '700' for '800'. The visual difference is negligible.
- **If needed, change to:**
  ```ts
  fontWeight: Platform.OS === 'android' ? '700' : '800',
  ```
- **Covers:** Audit issue 2.6.
- **Complexity:** S

### Fix 5.5: Share card modal back button during capture

- **File(s):** `components/share/ShareCardModal.tsx`
- **Current (line 58):** `onRequestClose={onClose}` -- correctly handled.
- **Enhancement:** Prevent back press during capture:
  ```tsx
  onRequestClose={sharing ? undefined : onClose}
  ```
  Change `undefined` to `() => {}` for Android safety:
  ```tsx
  onRequestClose={sharing ? () => {} : onClose}
  ```
- **Covers:** Audit issue 1.6.
- **Complexity:** S

### Fix 5.6: Reading menu z-index on Android

- **File(s):** `components/ReadingMenu.tsx`
- **Status:** LOW priority. Monitor for reports. The `elevation` and `zIndex` interaction on Android can be unpredictable but this uses a Modal which handles z-ordering correctly.
- **Covers:** Audit issue 1.7.
- **Complexity:** S (if needed)

---

## Summary

| Phase | Fix Count | Description |
|-------|-----------|-------------|
| Phase 1: Build & Config | 2 | Play Store URL, versionCode, bundle fonts |
| Phase 2: Critical Fixes | 4 | Georgia/Devanagari fonts, auth modal back, auth sheet padding |
| Phase 3: High-Priority | 6 | BackHandler (3 screens), Rate App store, letterSpacing, console.log |
| Phase 4: Medium | 7 | TextInput underline, Devanagari lineHeight, fetch timeout, poll interval, ripple, swipe threshold, safe area padding |
| Phase 5: Low/Deferred | 6 | Background audio, audio focus, ProGuard, fontWeight, share modal, reading menu |

- **Total fixes:** 25 (19 to implement now, 6 deferred)
- **New packages:** None (all deps already installed)
- **New files:**
  - `constants/fonts.ts` (new, ~30 lines)
  - `assets/fonts/NotoSansDevanagari-Regular.ttf` (download from Google Fonts)
  - `assets/fonts/NotoSansDevanagari-Bold.ttf` (download from Google Fonts)
  - `assets/fonts/NotoSerif-Regular.ttf` (download from Google Fonts)
  - `assets/fonts/NotoSerif-Italic.ttf` (download from Google Fonts)
  - `assets/fonts/NotoSerif-Bold.ttf` (download from Google Fonts)
  - `assets/fonts/NotoSerif-BoldItalic.ttf` (download from Google Fonts)
- **Estimated total lines changed:** ~350 (excluding font binary files)
- **Estimated implementation time:** 3-4 hours for Phases 1-4

---

## Dependency Graph

```
Fix 1.1 (config)
  └── Fix 3.4 (Rate App) depends on PLAY_STORE_URL

Fix 1.2 (bundle fonts)
  └── Fix 2.1 (Georgia/Devanagari replacement) depends on fonts being loaded

All other fixes are independent and can be implemented in any order.
```

---

*This plan is implementation-only. No code was modified. Agent 3 (The Executor) should implement fixes in phase order.*
